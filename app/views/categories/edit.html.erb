<h1>Editing Category</h1>

<%= render 'form', category: @category %>

<div class="container py-5">
    <%= link_to 'Show', category_path(@category), class: 'btn btn-success btn-lg' %> 
    <%= link_to 'Back', categories_path, class: 'btn btn-outline-dark btn-lg' %>
</div>
